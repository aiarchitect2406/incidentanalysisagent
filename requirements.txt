# Dependencies for running/deploying the AGENT itself locally — i.e. for
# `adk web .`, `adk run .`, deploy_skills_agent.py, publish_skills_to_registry.py,
# eval_run.py, and smoke_test.py, all run from the repo root.
#
# This is distinct from enterprise_support_agent/requirements.txt, which is
# scoped to the MCP gateway container only (mcp_server.py) and is installed
# separately, inside that container, by Cloud Run's buildpack — not on your
# local machine.
google-adk>=1.34.3,<2.0.0
google-cloud-aiplatform[adk,agent_engines]>=1.145.0,<2.0.0
google-cloud-secret-manager
google-auth
requests
